import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ttjb.printcalculator',
  appName: '3D Print Calculator',
  webDir: 'dist'
};

export default config;
